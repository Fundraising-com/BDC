@model IList<GA.BDC.Shared.Entities.BlogCategory>
<div class="panel panel-info" ng-controller="BlogCategoriesController as blogCategoriesCtrl">
    <div class="panel-heading">
        <h3 class="panel-title">Categories</h3>
    </div>
    <div class="panel-body">
        @*@foreach (var category in Model.OrderBy(p => p.Name))
        {
            <p style="border-bottom: solid 1px #EEE;"><a href="/blog/category/@category.Url">@category.Name</a></p>
        }*@
        <div ng-repeat="category in blogCategoriesCtrl.categories" ng-cloak>
            <p style="border-bottom: solid 1px #EEE;"><a ng-href="/blog/category/{{::category.Url}}">{{::category.Name}}</a></p>
        </div>
    </div>
</div>
