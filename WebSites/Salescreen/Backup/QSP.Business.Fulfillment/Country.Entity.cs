////////////////////////////////////////////////////////////////////////////////////////////
// Class generated by SqlCodeGen 1.1.0.0.
// Do not edit this file directly. Changes will be lost when this file is regenerated.
// Extensions should be added in a separate file using partial classes.
////////////////////////////////////////////////////////////////////////////////////////////
using System;
using System.Collections.Generic;
using System.Data;
using System.Text;
using NHibernate;
using NHibernate.Expression;
using NHibernate.Mapping.Attributes;

namespace QSP.Business.Fulfillment
{
	[Serializable]
	[Class(Schema="`dbo`", Table="`country`")]
	public partial class Country
	{
		#region Constants
        public const string CountryEntity = "Country";
		public const string CountryCodeProperty = "CountryCode";
		public const string CountryNameProperty = "CountryName";
		public const string DescriptiveInformationProperty = "DescriptiveInformation";
		#endregion

		#region Fields
		protected string countryCode = "";
		protected string countryName = "";
		protected string descriptiveInformation = "";
		#endregion

		#region Constructors
		public Country() 
		{
		}
		#endregion

		#region Properties
		[RawXml(Content=@"
		<id name=""CountryCode"" column=""`country_code`"">
			<generator class=""assigned"">
			</generator>
		</id>")]

		public virtual string CountryCode
		{
			get { return this.countryCode; }
			set { this.countryCode = value; }
		}

		[Property(Column="`country_name`")]
		public virtual string CountryName
		{
			get { return this.countryName; }
			set { this.countryName = value; }
		}

		[Property(Column="`descriptive_information`")]
		public virtual string DescriptiveInformation
		{
			get { return this.descriptiveInformation; }
			set { this.descriptiveInformation = value; }
		}
		#endregion

		#region Methods

        public static ICriteria CreateCriteria()
        {
            using (ISession session = SqlSessionManager.OpenSession())
            {
                ICriteria c = session.CreateCriteria(typeof(Country));
                return c;
            }
        }

        public static List<Country> GetCountryList(ICriteria criteria)
        {
            return (List<Country>)criteria.List<Country>();
        }

		public static Country GetCountry(string countryCode)
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				return session.Get<Country>(countryCode);
			}
		}

		public static List<Country> GetCountryList()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ICriteria c = session.CreateCriteria(typeof(Country));
				return (List<Country>)c.List<Country>();
			}
		}

		public static List<Country> GetCountryList(string sortExpression, int maximumRows, int startRowIndex)
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ICriteria c = session.CreateCriteria(typeof(Country));
				if (sortExpression != null && sortExpression != "")
				{
					// Get ascending or descending order
					bool descending = sortExpression.Contains(" DESC");

					// Strip off ASC or DESC ordering
					sortExpression = sortExpression.Replace(" ASC", "");
					sortExpression = sortExpression.Replace(" DESC", "");
					sortExpression = sortExpression.Trim();

					// Get multi column sort from the comma delimited string
					List<String> expressions = new List<String>();
					if (sortExpression.Contains(","))
					{
						string[] tokens = sortExpression.Split(",".ToCharArray());
						for (int i = 0; i < tokens.Length; i++)
						{
							tokens[i] = tokens[i].Trim();
							if (tokens[i] != "")
								expressions.Add(tokens[i]);
						}
					}
					else if (sortExpression != "")
					{
						expressions.Add(sortExpression);
					}

					// Create the order
					for (int i = 0; i < expressions.Count; i++)
					{
						if (descending)
							c.AddOrder(NHibernate.Expression.Order.Desc(expressions[i]));
						else
							c.AddOrder(NHibernate.Expression.Order.Asc(expressions[i]));
					}
				}

				// Set offset and limit
				if (startRowIndex >= 0)
					c.SetFirstResult(startRowIndex);

				if (maximumRows >= 0)
					c.SetMaxResults(maximumRows);

				return (List<Country>)c.List<Country>();
			}
		}

		public static List<Country> GetCountryList(string sortExpression)
		{
			return GetCountryList(sortExpression, -1, -1);
		}

		public static void InsertCountry(Country obj)
		{
			if (obj != null)
				obj.Insert();
		}

		public static void UpdateCountry(Country obj)
		{
			if (obj != null)
				obj.Update();
		}

		public static void DeleteCountry(Country obj)
		{
			if (obj != null)
				obj.Delete();
		}

        protected static Country PopulateCountry(IDataReader r)
		{
			Country obj = new Country();
			obj.CountryCode = (string)r["country_code"];
			obj.CountryName = (string)r["country_name"];
			obj.DescriptiveInformation = (string)r["descriptive_information"];

			return obj;
		}

		public static int GetCount()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ICriteria c = session.CreateCriteria(typeof(Country));
				c.SetProjection(Projections.RowCount());
				return (int) c.UniqueResult();
			}
		}

		/// <summary>
		/// Insert the entity to database.
		/// </summary>
		public virtual void Insert()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ITransaction trans = session.BeginTransaction();
				try
				{
					session.Save(this);
					trans.Commit();
				}
				catch
				{
					trans.Rollback();
					throw;
				}
			}
		}

		/// <summary>
		/// Update the entity to database.
		/// </summary>
		public virtual void Update()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ITransaction trans = session.BeginTransaction();
				try
				{
					session.Update(this);
					trans.Commit();
				}
				catch
				{
					trans.Rollback();
					throw;
				}
			}
		}

		/// <summary>
		/// Delete entity in database.
		/// </summary>
		public virtual void Delete()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ITransaction trans = session.BeginTransaction();
				try
				{
					session.Delete(this);
					trans.Commit();
				}
				catch
				{
					trans.Rollback();
					throw;
				}
			}
		}

		/// <summary>
		/// Local implementation of Equals based on unique value members
		/// </summary>
		public override bool Equals(object obj)
		{
			if (this == obj)
				return true;

			if ((obj == null) || (obj.GetType() != this.GetType())) 
				return false;

			Country castObj = (Country)obj;
			return (castObj != null && this.countryCode == castObj.CountryCode);
		}

		/// <summary>
		/// Local implementation of GetHashCode based on unique value members
		/// </summary>
		public override int GetHashCode()
		{
			return 29 * (1 + this.countryCode.GetHashCode());
		}
		#endregion
	}
}
