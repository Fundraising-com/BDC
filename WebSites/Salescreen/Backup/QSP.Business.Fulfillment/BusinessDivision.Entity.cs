////////////////////////////////////////////////////////////////////////////////////////////
// Class generated by SqlCodeGen 1.1.0.0.
// Do not edit this file directly. Changes will be lost when this file is regenerated.
// Extensions should be added in a separate file using partial classes.
////////////////////////////////////////////////////////////////////////////////////////////
using System;
using System.Collections.Generic;
using System.Data;
using System.Text;
using NHibernate;
using NHibernate.Expression;
using NHibernate.Mapping.Attributes;

namespace QSP.Business.Fulfillment
{
	[Serializable]
	[Class(Schema="`dbo`", Table="`business_division`")]
	public partial class BusinessDivision
	{
		#region Constants
        public const string BusinessDivisionEntity = "BusinessDivision";
		public const string BusinessDivisionIdProperty = "BusinessDivisionId";
		public const string BusinessDivisionNameProperty = "BusinessDivisionName";
		#endregion

		#region Fields
		protected int businessDivisionId = 0;
		protected string businessDivisionName = "";
		#endregion

		#region Constructors
		public BusinessDivision() 
		{
		}
		#endregion

		#region Properties
		[RawXml(Content=@"
		<id name=""BusinessDivisionId"" column=""`business_division_id`"">
			<generator class=""native"">
			</generator>
		</id>")]

		public virtual int BusinessDivisionId
		{
			get { return this.businessDivisionId; }
			set { this.businessDivisionId = value; }
		}

		[Property(Column="`business_division_name`")]
		public virtual string BusinessDivisionName
		{
			get { return this.businessDivisionName; }
			set { this.businessDivisionName = value; }
		}
		#endregion

		#region Methods

        public static ICriteria CreateCriteria()
        {
            using (ISession session = SqlSessionManager.OpenSession())
            {
                ICriteria c = session.CreateCriteria(typeof(BusinessDivision));
                return c;
            }
        }

        public static List<BusinessDivision> GetBusinessDivisionList(ICriteria criteria)
        {
            return (List<BusinessDivision>)criteria.List<BusinessDivision>();
        }

		public static BusinessDivision GetBusinessDivision(int businessDivisionId)
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				return session.Get<BusinessDivision>(businessDivisionId);
			}
		}

		public static List<BusinessDivision> GetBusinessDivisionList()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ICriteria c = session.CreateCriteria(typeof(BusinessDivision));
				return (List<BusinessDivision>)c.List<BusinessDivision>();
			}
		}

		public static List<BusinessDivision> GetBusinessDivisionList(string sortExpression, int maximumRows, int startRowIndex)
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ICriteria c = session.CreateCriteria(typeof(BusinessDivision));
				if (sortExpression != null && sortExpression != "")
				{
					// Get ascending or descending order
					bool descending = sortExpression.Contains(" DESC");

					// Strip off ASC or DESC ordering
					sortExpression = sortExpression.Replace(" ASC", "");
					sortExpression = sortExpression.Replace(" DESC", "");
					sortExpression = sortExpression.Trim();

					// Get multi column sort from the comma delimited string
					List<String> expressions = new List<String>();
					if (sortExpression.Contains(","))
					{
						string[] tokens = sortExpression.Split(",".ToCharArray());
						for (int i = 0; i < tokens.Length; i++)
						{
							tokens[i] = tokens[i].Trim();
							if (tokens[i] != "")
								expressions.Add(tokens[i]);
						}
					}
					else if (sortExpression != "")
					{
						expressions.Add(sortExpression);
					}

					// Create the order
					for (int i = 0; i < expressions.Count; i++)
					{
						if (descending)
							c.AddOrder(NHibernate.Expression.Order.Desc(expressions[i]));
						else
							c.AddOrder(NHibernate.Expression.Order.Asc(expressions[i]));
					}
				}

				// Set offset and limit
				if (startRowIndex >= 0)
					c.SetFirstResult(startRowIndex);

				if (maximumRows >= 0)
					c.SetMaxResults(maximumRows);

				return (List<BusinessDivision>)c.List<BusinessDivision>();
			}
		}

		public static List<BusinessDivision> GetBusinessDivisionList(string sortExpression)
		{
			return GetBusinessDivisionList(sortExpression, -1, -1);
		}

		public static void InsertBusinessDivision(BusinessDivision obj)
		{
			if (obj != null)
				obj.Insert();
		}

		public static void UpdateBusinessDivision(BusinessDivision obj)
		{
			if (obj != null)
				obj.Update();
		}

		public static void DeleteBusinessDivision(BusinessDivision obj)
		{
			if (obj != null)
				obj.Delete();
		}

        protected static BusinessDivision PopulateBusinessDivision(IDataReader r)
		{
			BusinessDivision obj = new BusinessDivision();
			obj.BusinessDivisionId = (int)r["business_division_id"];
			obj.BusinessDivisionName = (string)r["business_division_name"];

			return obj;
		}

		public static int GetCount()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ICriteria c = session.CreateCriteria(typeof(BusinessDivision));
				c.SetProjection(Projections.RowCount());
				return (int) c.UniqueResult();
			}
		}

		/// <summary>
		/// Insert the entity to database.
		/// </summary>
		public virtual void Insert()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ITransaction trans = session.BeginTransaction();
				try
				{
					session.Save(this);
					trans.Commit();
				}
				catch
				{
					trans.Rollback();
					throw;
				}
			}
		}

		/// <summary>
		/// Update the entity to database.
		/// </summary>
		public virtual void Update()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ITransaction trans = session.BeginTransaction();
				try
				{
					session.Update(this);
					trans.Commit();
				}
				catch
				{
					trans.Rollback();
					throw;
				}
			}
		}

		/// <summary>
		/// Persist the entity back to database.
		/// </summary>
		public virtual void Save()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ITransaction trans = session.BeginTransaction();
				try
				{
					session.SaveOrUpdate(this);
					trans.Commit();
				}
				catch
				{
					trans.Rollback();
					throw;
				}
			}
		}

		/// <summary>
		/// Delete entity in database.
		/// </summary>
		public virtual void Delete()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ITransaction trans = session.BeginTransaction();
				try
				{
					session.Delete(this);
					trans.Commit();
				}
				catch
				{
					trans.Rollback();
					throw;
				}
			}
		}

		/// <summary>
		/// Local implementation of Equals based on unique value members
		/// </summary>
		public override bool Equals(object obj)
		{
			if (this == obj)
				return true;

			if ((obj == null) || (obj.GetType() != this.GetType())) 
				return false;

			BusinessDivision castObj = (BusinessDivision)obj;
			return (castObj != null && this.businessDivisionId == castObj.BusinessDivisionId);
		}

		/// <summary>
		/// Local implementation of GetHashCode based on unique value members
		/// </summary>
		public override int GetHashCode()
		{
			return 29 * (1 + this.businessDivisionId.GetHashCode());
		}
		#endregion
	}
}
