////////////////////////////////////////////////////////////////////////////////////////////
// Class generated by SqlCodeGen 1.1.0.0.
// Do not edit this file directly. Changes will be lost when this file is regenerated.
// Extensions should be added in a separate file using partial classes.
////////////////////////////////////////////////////////////////////////////////////////////
using System;
using System.Collections.Generic;
using System.Data;
using System.Text;
using NHibernate;
using NHibernate.Criterion;
using NHibernate.Mapping.Attributes;

namespace QSP.Business.Fulfillment
{
	[Serializable]
	[Class(Schema="`dbo`", Table="`installment_plan`")]
	public partial class InstallmentPlan
	{
		#region Constants
        public const string InstallmentPlanEntity = "InstallmentPlan";
		public const string InstallmentPlanIdProperty = "InstallmentPlanId";
		public const string InstallmentPlanNameProperty = "InstallmentPlanName";
		#endregion

		#region Fields
		protected int installmentPlanId = 0;
		protected string installmentPlanName = "";
		#endregion

		#region Constructors
		public InstallmentPlan() 
		{
		}
		#endregion

		#region Properties
		[RawXml(Content=@"
		<id name=""InstallmentPlanId"" column=""`installment_plan_id`"">
			<generator class=""assigned"">
			</generator>
		</id>")]

		public virtual int InstallmentPlanId
		{
			get { return this.installmentPlanId; }
			set { this.installmentPlanId = value; }
		}

		[Property(Column="`installment_plan_name`")]
		public virtual string InstallmentPlanName
		{
			get { return this.installmentPlanName; }
			set { this.installmentPlanName = value; }
		}
		#endregion

		#region Methods

        public static ICriteria CreateCriteria()
        {
            using (ISession session = SqlSessionManager.OpenSession())
            {
                ICriteria c = session.CreateCriteria(typeof(InstallmentPlan));
                return c;
            }
        }

        public static List<InstallmentPlan> GetInstallmentPlanList(ICriteria criteria)
        {
            return (List<InstallmentPlan>)criteria.List<InstallmentPlan>();
        }

		public static InstallmentPlan GetInstallmentPlan(int installmentPlanId)
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				return session.Get<InstallmentPlan>(installmentPlanId);
			}
		}

		public static List<InstallmentPlan> GetInstallmentPlanList()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ICriteria c = session.CreateCriteria(typeof(InstallmentPlan));
				return (List<InstallmentPlan>)c.List<InstallmentPlan>();
			}
		}

		public static List<InstallmentPlan> GetInstallmentPlanList(string sortExpression, int maximumRows, int startRowIndex)
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ICriteria c = session.CreateCriteria(typeof(InstallmentPlan));
				if (sortExpression != null && sortExpression != "")
				{
					// Get ascending or descending order
					bool descending = sortExpression.Contains(" DESC");

					// Strip off ASC or DESC ordering
					sortExpression = sortExpression.Replace(" ASC", "");
					sortExpression = sortExpression.Replace(" DESC", "");
					sortExpression = sortExpression.Trim();

					// Get multi column sort from the comma delimited string
					List<String> expressions = new List<String>();
					if (sortExpression.Contains(","))
					{
						string[] tokens = sortExpression.Split(",".ToCharArray());
						for (int i = 0; i < tokens.Length; i++)
						{
							tokens[i] = tokens[i].Trim();
							if (tokens[i] != "")
								expressions.Add(tokens[i]);
						}
					}
					else if (sortExpression != "")
					{
						expressions.Add(sortExpression);
					}

					// Create the order
					for (int i = 0; i < expressions.Count; i++)
					{
						if (descending)
							c.AddOrder(NHibernate.Criterion.Order.Desc(expressions[i]));
						else
							c.AddOrder(NHibernate.Criterion.Order.Asc(expressions[i]));
					}
				}

				// Set offset and limit
				if (startRowIndex >= 0)
					c.SetFirstResult(startRowIndex);

				if (maximumRows >= 0)
					c.SetMaxResults(maximumRows);

				return (List<InstallmentPlan>)c.List<InstallmentPlan>();
			}
		}

		public static List<InstallmentPlan> GetInstallmentPlanList(string sortExpression)
		{
			return GetInstallmentPlanList(sortExpression, -1, -1);
		}

		public static void InsertInstallmentPlan(InstallmentPlan obj)
		{
			if (obj != null)
				obj.Insert();
		}

		public static void UpdateInstallmentPlan(InstallmentPlan obj)
		{
			if (obj != null)
				obj.Update();
		}

		public static void DeleteInstallmentPlan(InstallmentPlan obj)
		{
			if (obj != null)
				obj.Delete();
		}

        protected static InstallmentPlan PopulateInstallmentPlan(IDataReader r)
		{
			InstallmentPlan obj = new InstallmentPlan();
			obj.InstallmentPlanId = (int)r["installment_plan_id"];
			obj.InstallmentPlanName = (string)r["installment_plan_name"];

			return obj;
		}

		public static int GetCount()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ICriteria c = session.CreateCriteria(typeof(InstallmentPlan));
				c.SetProjection(Projections.RowCount());
				return (int) c.UniqueResult();
			}
		}

		/// <summary>
		/// Insert the entity to database.
		/// </summary>
		public virtual void Insert()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ITransaction trans = session.BeginTransaction();
				try
				{
					session.Save(this);
					trans.Commit();
				}
				catch
				{
					trans.Rollback();
					throw;
				}
			}
		}

		/// <summary>
		/// Update the entity to database.
		/// </summary>
		public virtual void Update()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ITransaction trans = session.BeginTransaction();
				try
				{
					session.Update(this);
					trans.Commit();
				}
				catch
				{
					trans.Rollback();
					throw;
				}
			}
		}

		/// <summary>
		/// Delete entity in database.
		/// </summary>
		public virtual void Delete()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ITransaction trans = session.BeginTransaction();
				try
				{
					session.Delete(this);
					trans.Commit();
				}
				catch
				{
					trans.Rollback();
					throw;
				}
			}
		}

		/// <summary>
		/// Local implementation of Equals based on unique value members
		/// </summary>
		public override bool Equals(object obj)
		{
			if (this == obj)
				return true;

			if ((obj == null) || (obj.GetType() != this.GetType())) 
				return false;

			InstallmentPlan castObj = (InstallmentPlan)obj;
			return (castObj != null && this.installmentPlanId == castObj.InstallmentPlanId);
		}

		/// <summary>
		/// Local implementation of GetHashCode based on unique value members
		/// </summary>
		public override int GetHashCode()
		{
			return 29 * (1 + this.installmentPlanId.GetHashCode());
		}
		#endregion
	}
}
