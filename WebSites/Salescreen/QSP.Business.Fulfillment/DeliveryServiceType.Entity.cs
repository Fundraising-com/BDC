////////////////////////////////////////////////////////////////////////////////////////////
// Class generated by SqlCodeGen 1.1.0.0.
// Do not edit this file directly. Changes will be lost when this file is regenerated.
// Extensions should be added in a separate file using partial classes.
////////////////////////////////////////////////////////////////////////////////////////////
using System;
using System.Collections.Generic;
using System.Data;
using System.Text;
using NHibernate;
using NHibernate.Criterion;
using NHibernate.Mapping.Attributes;

namespace QSP.Business.Fulfillment
{
    [Serializable]
    [Class(Schema = "`dbo`", Table = "`delivery_service_type`")]
    public partial class DeliveryServiceType
    {
        #region Constants
        public const string DeliveryServiceTypeEntity = "DeliveryServiceType";
        public const string DeliveryServiceTypeIdProperty = "DeliveryServiceTypeId";
        public const string NameProperty = "Name";
        #endregion

        #region Fields
        protected int deliveryServiceTypeId = 0;
        protected string name = null;
        #endregion

        #region Constructors
        public DeliveryServiceType()
        {
        }
        #endregion

        #region Properties
        [RawXml(Content = @"
		<id name=""DeliveryServiceTypeId"" column=""`delivery_service_type_id`"">
			<generator class=""assigned"">
			</generator>
		</id>")]

        public virtual int DeliveryServiceTypeId
        {
            get { return this.deliveryServiceTypeId; }
            set { this.deliveryServiceTypeId = value; }
        }

        [Property(Column = "`name`")]
        public virtual string Name
        {
            get { return this.name; }
            set { this.name = value; }
        }
        #endregion

        #region Methods
        public static DeliveryServiceType GetDeliveryServiceType(int deliveryServiceTypeId)
        {
            using (ISession session = SqlSessionManager.OpenSession())
            {
                return session.Get<DeliveryServiceType>(deliveryServiceTypeId);
            }
        }

        public static List<DeliveryServiceType> GetDeliveryServiceTypeList(ICriteria criteria)
        {
            return (List<DeliveryServiceType>)criteria.List<DeliveryServiceType>();
        }

        public static List<DeliveryServiceType> GetDeliveryServiceTypeList()
        {
            using (ISession session = SqlSessionManager.OpenSession())
            {
                ICriteria c = session.CreateCriteria(typeof(DeliveryServiceType));
                return (List<DeliveryServiceType>)c.List<DeliveryServiceType>();
            }
        }

        public static List<DeliveryServiceType> GetDeliveryServiceTypeList(string sortExpression, int maximumRows, int startRowIndex)
        {
            using (ISession session = SqlSessionManager.OpenSession())
            {
                ICriteria c = session.CreateCriteria(typeof(DeliveryServiceType));
                if (sortExpression != null && sortExpression != "")
                {
                    // Get ascending or descending order
                    bool descending = sortExpression.Contains(" DESC");

                    // Strip off ASC or DESC ordering
                    sortExpression = sortExpression.Replace(" ASC", "");
                    sortExpression = sortExpression.Replace(" DESC", "");
                    sortExpression = sortExpression.Trim();

                    // Get multi column sort from the comma delimited string
                    List<String> expressions = new List<String>();
                    if (sortExpression.Contains(","))
                    {
                        string[] tokens = sortExpression.Split(",".ToCharArray());
                        for (int i = 0; i < tokens.Length; i++)
                        {
                            tokens[i] = tokens[i].Trim();
                            if (tokens[i] != "")
                                expressions.Add(tokens[i]);
                        }
                    }
                    else if (sortExpression != "")
                    {
                        expressions.Add(sortExpression);
                    }

                    // Create the order
                    for (int i = 0; i < expressions.Count; i++)
                    {
                        if (descending)
                            c.AddOrder(NHibernate.Criterion.Order.Desc(expressions[i]));
                        else
                            c.AddOrder(NHibernate.Criterion.Order.Asc(expressions[i]));
                    }
                }

                // Set offset and limit
                if (startRowIndex >= 0)
                    c.SetFirstResult(startRowIndex);

                if (maximumRows >= 0)
                    c.SetMaxResults(maximumRows);

                return (List<DeliveryServiceType>)c.List<DeliveryServiceType>();
            }
        }

        public static List<DeliveryServiceType> GetDeliveryServiceTypeList(string sortExpression)
        {
            return GetDeliveryServiceTypeList(sortExpression, -1, -1);
        }

        public static void InsertDeliveryServiceType(DeliveryServiceType obj)
        {
            if (obj != null)
                obj.Insert();
        }

        public static void UpdateDeliveryServiceType(DeliveryServiceType obj)
        {
            if (obj != null)
                obj.Update();
        }

        public static void DeleteDeliveryServiceType(DeliveryServiceType obj)
        {
            if (obj != null)
                obj.Delete();
        }

        protected static DeliveryServiceType PopulateDeliveryServiceType(IDataReader r)
        {
            DeliveryServiceType obj = new DeliveryServiceType();
            obj.DeliveryServiceTypeId = (int)r["delivery_service_type_id"];
            obj.Name = (r["name"] == DBNull.Value) ? null : (string)r["name"];

            return obj;
        }

        public static int GetCount()
        {
            using (ISession session = SqlSessionManager.OpenSession())
            {
                ICriteria c = session.CreateCriteria(typeof(DeliveryServiceType));
                c.SetProjection(Projections.RowCount());
                return (int)c.UniqueResult();
            }
        }

        /// <summary>
        /// Insert the entity to database.
        /// </summary>
        public virtual void Insert()
        {
            using (ISession session = SqlSessionManager.OpenSession())
            {
                ITransaction trans = session.BeginTransaction();
                try
                {
                    session.Save(this);
                    trans.Commit();
                }
                catch
                {
                    trans.Rollback();
                    throw;
                }
            }
        }

        /// <summary>
        /// Update the entity to database.
        /// </summary>
        public virtual void Update()
        {
            using (ISession session = SqlSessionManager.OpenSession())
            {
                ITransaction trans = session.BeginTransaction();
                try
                {
                    session.Update(this);
                    trans.Commit();
                }
                catch
                {
                    trans.Rollback();
                    throw;
                }
            }
        }

        /// <summary>
        /// Delete entity in database.
        /// </summary>
        public virtual void Delete()
        {
            using (ISession session = SqlSessionManager.OpenSession())
            {
                ITransaction trans = session.BeginTransaction();
                try
                {
                    session.Delete(this);
                    trans.Commit();
                }
                catch
                {
                    trans.Rollback();
                    throw;
                }
            }
        }

        /// <summary>
        /// Local implementation of Equals based on unique value members
        /// </summary>
        public override bool Equals(object obj)
        {
            if (this == obj)
                return true;

            if ((obj == null) || (obj.GetType() != this.GetType()))
                return false;

            DeliveryServiceType castObj = (DeliveryServiceType)obj;
            return (castObj != null && this.deliveryServiceTypeId == castObj.DeliveryServiceTypeId);
        }

        /// <summary>
        /// Local implementation of GetHashCode based on unique value members
        /// </summary>
        public override int GetHashCode()
        {
            return 29 * (1 + this.deliveryServiceTypeId.GetHashCode());
        }
        #endregion
    }
}
