////////////////////////////////////////////////////////////////////////////////////////////
// Class generated by SqlCodeGen 1.1.0.0.
// Do not edit this file directly. Changes will be lost when this file is regenerated.
// Extensions should be added in a separate file using partial classes.
////////////////////////////////////////////////////////////////////////////////////////////
using System;
using System.Collections.Generic;
using System.Data;
using System.Text;
using NHibernate;
using NHibernate.Criterion;
using NHibernate.Mapping.Attributes;

namespace QSP.Business.Fulfillment
{
    [Serializable]
    [Class(Schema = "`dbo`", Table = "`commission`")]
    public partial class Commission
    {
        #region Constants
        public const string CommissionEntity = "Commission";
        public const string CommissionIdProperty = "CommissionId";
        public const string OrderDetailIdProperty = "OrderDetailId";
        public const string CommissionRateProperty = "CommissionRate";
        public const string CommissionTypeIdProperty = "CommissionTypeId";
        public const string FmIdProperty = "FmId";
        public const string CreateDateProperty = "CreateDate";
        public const string CreateUserIdProperty = "CreateUserId";
        public const string UpdateDateProperty = "UpdateDate";
        public const string UpdateUserIdProperty = "UpdateUserId";
        #endregion

        #region Fields
        protected int commissionId = 0;
        protected int orderDetailId = 0;
        protected decimal commissionRate = 0;
        protected int commissionTypeId = 0;
        protected string fmId = "";
        protected DateTime createDate = DateTime.Now;
        protected int createUserId = 0;
        protected DateTime? updateDate = null;
        protected int? updateUserId = null;
        #endregion

        #region Constructors
        public Commission()
        {
        }
        #endregion

        #region Properties
        [RawXml(Content = @"
		<id name=""CommissionId"" column=""`commission_id`"">
			<generator class=""native"">
			</generator>
		</id>")]

        public virtual int CommissionId
        {
            get { return this.commissionId; }
            set { this.commissionId = value; }
        }

        [Property(Column = "`order_detail_id`")]
        public virtual int OrderDetailId
        {
            get { return this.orderDetailId; }
            set { this.orderDetailId = value; }
        }

        [Property(Column = "`commission_rate`")]
        public virtual decimal CommissionRate
        {
            get { return this.commissionRate; }
            set { this.commissionRate = value; }
        }

        [Property(Column = "`commission_type_id`")]
        public virtual int CommissionTypeId
        {
            get { return this.commissionTypeId; }
            set { this.commissionTypeId = value; }
        }

        [Property(Column = "`fm_id`")]
        public virtual string FmId
        {
            get { return this.fmId; }
            set { this.fmId = value; }
        }

        [Property(Column = "`create_date`")]
        public virtual DateTime CreateDate
        {
            get { return this.createDate; }
            set { this.createDate = value; }
        }

        [Property(Column = "`create_user_id`")]
        public virtual int CreateUserId
        {
            get { return this.createUserId; }
            set { this.createUserId = value; }
        }

        [Property(Column = "`update_date`")]
        public virtual DateTime? UpdateDate
        {
            get { return this.updateDate; }
            set { this.updateDate = value; }
        }

        [Property(Column = "`update_user_id`")]
        public virtual int? UpdateUserId
        {
            get { return this.updateUserId; }
            set { this.updateUserId = value; }
        }
        #endregion

        #region Methods

        public static ICriteria CreateCriteria()
        {
            using (ISession session = SqlSessionManager.OpenSession())
            {
                ICriteria c = session.CreateCriteria(typeof(Commission));
                return c;
            }
        }


        public static Commission GetCommission(int commissionId)
        {
            using (ISession session = SqlSessionManager.OpenSession())
            {
                return session.Get<Commission>(commissionId);
            }
        }

        public static List<Commission> GetCommissionList()
        {
            using (ISession session = SqlSessionManager.OpenSession())
            {
                ICriteria c = session.CreateCriteria(typeof(Commission));
                return (List<Commission>)c.List<Commission>();
            }
        }

        public static List<Commission> GetCommissionList(string sortExpression, int maximumRows, int startRowIndex)
        {
            using (ISession session = SqlSessionManager.OpenSession())
            {
                ICriteria c = session.CreateCriteria(typeof(Commission));
                if (sortExpression != null && sortExpression != "")
                {
                    // Get ascending or descending order
                    bool descending = sortExpression.Contains(" DESC");

                    // Strip off ASC or DESC ordering
                    sortExpression = sortExpression.Replace(" ASC", "");
                    sortExpression = sortExpression.Replace(" DESC", "");
                    sortExpression = sortExpression.Trim();

                    // Get multi column sort from the comma delimited string
                    List<String> expressions = new List<String>();
                    if (sortExpression.Contains(","))
                    {
                        string[] tokens = sortExpression.Split(",".ToCharArray());
                        for (int i = 0; i < tokens.Length; i++)
                        {
                            tokens[i] = tokens[i].Trim();
                            if (tokens[i] != "")
                                expressions.Add(tokens[i]);
                        }
                    }
                    else if (sortExpression != "")
                    {
                        expressions.Add(sortExpression);
                    }

                    // Create the order
                    for (int i = 0; i < expressions.Count; i++)
                    {
                        if (descending)
                            c.AddOrder(NHibernate.Criterion.Order.Desc(expressions[i]));
                        else
                            c.AddOrder(NHibernate.Criterion.Order.Asc(expressions[i]));
                    }
                }

                // Set offset and limit
                if (startRowIndex >= 0)
                    c.SetFirstResult(startRowIndex);

                if (maximumRows >= 0)
                    c.SetMaxResults(maximumRows);

                return (List<Commission>)c.List<Commission>();
            }
        }

        public static List<Commission> GetCommissionList(string sortExpression)
        {
            return GetCommissionList(sortExpression, -1, -1);
        }

        public static List<Commission> GetCommissionList(ICriteria criteria)
        {
            return (List<Commission>)criteria.List<Commission>();
        }

        public static void InsertCommission(Commission obj)
        {
            if (obj != null)
                obj.Insert();
        }

        public static void UpdateCommission(Commission obj)
        {
            if (obj != null)
                obj.Update();
        }

        public static void DeleteCommission(Commission obj)
        {
            if (obj != null)
                obj.Delete();
        }

        protected static Commission PopulateCommission(IDataReader r)
        {
            Commission obj = new Commission();
            obj.CommissionId = (int)r["commission_id"];
            obj.OrderDetailId = (int)r["order_detail_id"];
            obj.CommissionRate = (decimal)r["commission_rate"];
            obj.CommissionTypeId = (int)r["commission_type_id"];
            obj.FmId = (string)r["fm_id"];
            obj.CreateDate = (DateTime)r["create_date"];
            obj.CreateUserId = (int)r["create_user_id"];
            obj.UpdateDate = (r["update_date"] == DBNull.Value) ? null : (DateTime?)r["update_date"];
            obj.UpdateUserId = (r["update_user_id"] == DBNull.Value) ? null : (int?)r["update_user_id"];

            return obj;
        }

        public static int GetCount()
        {
            using (ISession session = SqlSessionManager.OpenSession())
            {
                ICriteria c = session.CreateCriteria(typeof(Commission));
                c.SetProjection(Projections.RowCount());
                return (int)c.UniqueResult();
            }
        }

        /// <summary>
        /// Insert the entity to database.
        /// </summary>
        public virtual void Insert()
        {
            using (ISession session = SqlSessionManager.OpenSession())
            {
                ITransaction trans = session.BeginTransaction();
                try
                {
                    session.Save(this);
                    trans.Commit();
                }
                catch
                {
                    trans.Rollback();
                    throw;
                }
            }
        }

        /// <summary>
        /// Update the entity to database.
        /// </summary>
        public virtual void Update()
        {
            using (ISession session = SqlSessionManager.OpenSession())
            {
                ITransaction trans = session.BeginTransaction();
                try
                {
                    session.Update(this);
                    trans.Commit();
                }
                catch
                {
                    trans.Rollback();
                    throw;
                }
            }
        }

        /// <summary>
        /// Persist the entity back to database.
        /// </summary>
        public virtual void Save()
        {
            using (ISession session = SqlSessionManager.OpenSession())
            {
                ITransaction trans = session.BeginTransaction();
                try
                {
                    session.SaveOrUpdate(this);
                    trans.Commit();
                }
                catch
                {
                    trans.Rollback();
                    throw;
                }
            }
        }

        /// <summary>
        /// Delete entity in database.
        /// </summary>
        public virtual void Delete()
        {
            using (ISession session = SqlSessionManager.OpenSession())
            {
                ITransaction trans = session.BeginTransaction();
                try
                {
                    session.Delete(this);
                    trans.Commit();
                }
                catch
                {
                    trans.Rollback();
                    throw;
                }
            }
        }

        /// <summary>
        /// Local implementation of Equals based on unique value members
        /// </summary>
        public override bool Equals(object obj)
        {
            if (this == obj)
                return true;

            if ((obj == null) || (obj.GetType() != this.GetType()))
                return false;

            Commission castObj = (Commission)obj;
            return (castObj != null && this.commissionId == castObj.CommissionId);
        }

        /// <summary>
        /// Local implementation of GetHashCode based on unique value members
        /// </summary>
        public override int GetHashCode()
        {
            return 29 * (1 + this.commissionId.GetHashCode());
        }

        #endregion
    }
}