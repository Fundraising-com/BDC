////////////////////////////////////////////////////////////////////////////////////////////
// Class generated by SqlCodeGen 1.1.0.0.
// Do not edit this file directly. Changes will be lost when this file is regenerated.
// Extensions should be added in a separate file using partial classes.
////////////////////////////////////////////////////////////////////////////////////////////
using System;
using System.Collections.Generic;
using System.Data;
using System.Text;
using NHibernate;
using NHibernate.Criterion;
using NHibernate.Mapping.Attributes;

namespace QSP.Business.Fulfillment
{
	[Serializable]
	[Class(Schema="`dbo`", Table="`product_line`")]
	public partial class ProductLine
	{
		#region Constants
        public const string ProductLineEntity = "ProductLine";
		public const string ProductLineIdProperty = "ProductLineId";
		public const string ProductLineNameProperty = "ProductLineName";
		#endregion

		#region Fields
		protected int productLineId = 0;
		protected string productLineName = "";
		#endregion

		#region Constructors
		public ProductLine() 
		{
		}
		#endregion

		#region Properties
		[RawXml(Content=@"
		<id name=""ProductLineId"" column=""`product_line_id`"">
			<generator class=""assigned"">
			</generator>
		</id>")]

		public virtual int ProductLineId
		{
			get { return this.productLineId; }
			set { this.productLineId = value; }
		}

		[Property(Column="`product_line_name`")]
		public virtual string ProductLineName
		{
			get { return this.productLineName; }
			set { this.productLineName = value; }
		}
		#endregion

		#region Methods

        public static ICriteria CreateCriteria()
        {
            using (ISession session = SqlSessionManager.OpenSession())
            {
                ICriteria c = session.CreateCriteria(typeof(ProductLine));
                return c;
            }
        }

        public static List<ProductLine> GetProductLineList(ICriteria criteria)
        {
            return (List<ProductLine>)criteria.List<ProductLine>();
        }

		public static ProductLine GetProductLine(int productLineId)
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				return session.Get<ProductLine>(productLineId);
			}
		}

		public static List<ProductLine> GetProductLineList()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ICriteria c = session.CreateCriteria(typeof(ProductLine));
				return (List<ProductLine>)c.List<ProductLine>();
			}
		}

		public static List<ProductLine> GetProductLineList(string sortExpression, int maximumRows, int startRowIndex)
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ICriteria c = session.CreateCriteria(typeof(ProductLine));
				if (sortExpression != null && sortExpression != "")
				{
					// Get ascending or descending order
					bool descending = sortExpression.Contains(" DESC");

					// Strip off ASC or DESC ordering
					sortExpression = sortExpression.Replace(" ASC", "");
					sortExpression = sortExpression.Replace(" DESC", "");
					sortExpression = sortExpression.Trim();

					// Get multi column sort from the comma delimited string
					List<String> expressions = new List<String>();
					if (sortExpression.Contains(","))
					{
						string[] tokens = sortExpression.Split(",".ToCharArray());
						for (int i = 0; i < tokens.Length; i++)
						{
							tokens[i] = tokens[i].Trim();
							if (tokens[i] != "")
								expressions.Add(tokens[i]);
						}
					}
					else if (sortExpression != "")
					{
						expressions.Add(sortExpression);
					}

					// Create the order
					for (int i = 0; i < expressions.Count; i++)
					{
						if (descending)
							c.AddOrder(NHibernate.Criterion.Order.Desc(expressions[i]));
						else
							c.AddOrder(NHibernate.Criterion.Order.Asc(expressions[i]));
					}
				}

				// Set offset and limit
				if (startRowIndex >= 0)
					c.SetFirstResult(startRowIndex);

				if (maximumRows >= 0)
					c.SetMaxResults(maximumRows);

				return (List<ProductLine>)c.List<ProductLine>();
			}
		}

		public static List<ProductLine> GetProductLineList(string sortExpression)
		{
			return GetProductLineList(sortExpression, -1, -1);
		}

		public static void InsertProductLine(ProductLine obj)
		{
			if (obj != null)
				obj.Insert();
		}

		public static void UpdateProductLine(ProductLine obj)
		{
			if (obj != null)
				obj.Update();
		}

		public static void DeleteProductLine(ProductLine obj)
		{
			if (obj != null)
				obj.Delete();
		}

        protected static ProductLine PopulateProductLine(IDataReader r)
		{
			ProductLine obj = new ProductLine();
			obj.ProductLineId = (int)r["product_line_id"];
			obj.ProductLineName = (string)r["product_line_name"];

			return obj;
		}

		public static int GetCount()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ICriteria c = session.CreateCriteria(typeof(ProductLine));
				c.SetProjection(Projections.RowCount());
				return (int) c.UniqueResult();
			}
		}

		/// <summary>
		/// Insert the entity to database.
		/// </summary>
		public virtual void Insert()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ITransaction trans = session.BeginTransaction();
				try
				{
					session.Save(this);
					trans.Commit();
				}
				catch
				{
					trans.Rollback();
					throw;
				}
			}
		}

		/// <summary>
		/// Update the entity to database.
		/// </summary>
		public virtual void Update()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ITransaction trans = session.BeginTransaction();
				try
				{
					session.Update(this);
					trans.Commit();
				}
				catch
				{
					trans.Rollback();
					throw;
				}
			}
		}

		/// <summary>
		/// Delete entity in database.
		/// </summary>
		public virtual void Delete()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ITransaction trans = session.BeginTransaction();
				try
				{
					session.Delete(this);
					trans.Commit();
				}
				catch
				{
					trans.Rollback();
					throw;
				}
			}
		}

		/// <summary>
		/// Local implementation of Equals based on unique value members
		/// </summary>
		public override bool Equals(object obj)
		{
			if (this == obj)
				return true;

			if ((obj == null) || (obj.GetType() != this.GetType())) 
				return false;

			ProductLine castObj = (ProductLine)obj;
			return (castObj != null && this.productLineId == castObj.ProductLineId);
		}

		/// <summary>
		/// Local implementation of GetHashCode based on unique value members
		/// </summary>
		public override int GetHashCode()
		{
			return 29 * (1 + this.productLineId.GetHashCode());
		}
		#endregion
	}
}
