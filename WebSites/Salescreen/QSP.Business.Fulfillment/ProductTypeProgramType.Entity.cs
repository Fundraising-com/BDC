////////////////////////////////////////////////////////////////////////////////////////////
// Class generated by SqlCodeGen 1.1.0.0.
// Do not edit this file directly. Changes will be lost when this file is regenerated.
// Extensions should be added in a separate file using partial classes.
////////////////////////////////////////////////////////////////////////////////////////////
using System;
using System.Collections.Generic;
using System.Data;
using System.Text;
using NHibernate;
using NHibernate.Criterion;
using NHibernate.Mapping.Attributes;

namespace QSP.Business.Fulfillment
{
	[Serializable]
	[Class(Schema="`dbo`", Table="`product_type_program_type`")]
	public partial class ProductTypeProgramType
	{
		#region Constants
        public const string ProductTypeProgramTypeEntity = "ProductTypeProgramType";
		public const string ProductTypeIdProperty = "ProductTypeId";
		public const string ProgramTypeIdProperty = "ProgramTypeId";
		#endregion

		#region Fields
		protected int productTypeId = 0;
		protected int programTypeId = 0;
		#endregion

		#region Constructors
		public ProductTypeProgramType() 
		{
		}
		#endregion

		#region Properties
		[RawXml(Content=@"
		<composite-id>
			<key-property name=""ProductTypeId"" column=""`product_type_id`"" />
			<key-property name=""ProgramTypeId"" column=""`program_type_id`"" />
		</composite-id>")]

		public virtual int ProductTypeId
		{
			get { return this.productTypeId; }
			set { this.productTypeId = value; }
		}

		public virtual int ProgramTypeId
		{
			get { return this.programTypeId; }
			set { this.programTypeId = value; }
		}
		#endregion

		#region Methods

        public static ICriteria CreateCriteria()
        {
            using (ISession session = SqlSessionManager.OpenSession())
            {
                ICriteria c = session.CreateCriteria(typeof(ProductTypeProgramType));
                return c;
            }
        }

        public static List<ProductTypeProgramType> GetProductTypeProgramTypeList(ICriteria criteria)
        {
            return (List<ProductTypeProgramType>)criteria.List<ProductTypeProgramType>();
        }

		public static ProductTypeProgramType GetProductTypeProgramType(int productTypeId, int programTypeId)
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ICriteria c = session.CreateCriteria(typeof(ProductTypeProgramType));
				c.Add(Expression.Eq(ProductTypeIdProperty, productTypeId));
				c.Add(Expression.Eq(ProgramTypeIdProperty, programTypeId));
				return c.UniqueResult<ProductTypeProgramType>();
			}
		}

		public static List<ProductTypeProgramType> GetProductTypeProgramTypeList()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ICriteria c = session.CreateCriteria(typeof(ProductTypeProgramType));
				return (List<ProductTypeProgramType>)c.List<ProductTypeProgramType>();
			}
		}

		public static List<ProductTypeProgramType> GetProductTypeProgramTypeList(string sortExpression, int maximumRows, int startRowIndex)
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ICriteria c = session.CreateCriteria(typeof(ProductTypeProgramType));
				if (sortExpression != null && sortExpression != "")
				{
					// Get ascending or descending order
					bool descending = sortExpression.Contains(" DESC");

					// Strip off ASC or DESC ordering
					sortExpression = sortExpression.Replace(" ASC", "");
					sortExpression = sortExpression.Replace(" DESC", "");
					sortExpression = sortExpression.Trim();

					// Get multi column sort from the comma delimited string
					List<String> expressions = new List<String>();
					if (sortExpression.Contains(","))
					{
						string[] tokens = sortExpression.Split(",".ToCharArray());
						for (int i = 0; i < tokens.Length; i++)
						{
							tokens[i] = tokens[i].Trim();
							if (tokens[i] != "")
								expressions.Add(tokens[i]);
						}
					}
					else if (sortExpression != "")
					{
						expressions.Add(sortExpression);
					}

					// Create the order
					for (int i = 0; i < expressions.Count; i++)
					{
						if (descending)
							c.AddOrder(NHibernate.Criterion.Order.Desc(expressions[i]));
						else
							c.AddOrder(NHibernate.Criterion.Order.Asc(expressions[i]));
					}
				}

				// Set offset and limit
				if (startRowIndex >= 0)
					c.SetFirstResult(startRowIndex);

				if (maximumRows >= 0)
					c.SetMaxResults(maximumRows);

				return (List<ProductTypeProgramType>)c.List<ProductTypeProgramType>();
			}
		}

		public static List<ProductTypeProgramType> GetProductTypeProgramTypeList(string sortExpression)
		{
			return GetProductTypeProgramTypeList(sortExpression, -1, -1);
		}

		public static void InsertProductTypeProgramType(ProductTypeProgramType obj)
		{
			if (obj != null)
				obj.Insert();
		}

		public static void UpdateProductTypeProgramType(ProductTypeProgramType obj)
		{
			if (obj != null)
				obj.Update();
		}

		public static void DeleteProductTypeProgramType(ProductTypeProgramType obj)
		{
			if (obj != null)
				obj.Delete();
		}

        protected static ProductTypeProgramType PopulateProductTypeProgramType(IDataReader r)
		{
			ProductTypeProgramType obj = new ProductTypeProgramType();
			obj.ProductTypeId = (int)r["product_type_id"];
			obj.ProgramTypeId = (int)r["program_type_id"];

			return obj;
		}

		public static int GetCount()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ICriteria c = session.CreateCriteria(typeof(ProductTypeProgramType));
				c.SetProjection(Projections.RowCount());
				return (int) c.UniqueResult();
			}
		}

		/// <summary>
		/// Insert the entity to database.
		/// </summary>
		public virtual void Insert()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ITransaction trans = session.BeginTransaction();
				try
				{
					session.Save(this);
					trans.Commit();
				}
				catch
				{
					trans.Rollback();
					throw;
				}
			}
		}

		/// <summary>
		/// Update the entity to database.
		/// </summary>
		public virtual void Update()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ITransaction trans = session.BeginTransaction();
				try
				{
					session.Update(this);
					trans.Commit();
				}
				catch
				{
					trans.Rollback();
					throw;
				}
			}
		}

		/// <summary>
		/// Delete entity in database.
		/// </summary>
		public virtual void Delete()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ITransaction trans = session.BeginTransaction();
				try
				{
					session.Delete(this);
					trans.Commit();
				}
				catch
				{
					trans.Rollback();
					throw;
				}
			}
		}

		/// <summary>
		/// Local implementation of Equals based on unique value members
		/// </summary>
		public override bool Equals(object obj)
		{
			if (this == obj)
				return true;

			if ((obj == null) || (obj.GetType() != this.GetType())) 
				return false;

			ProductTypeProgramType castObj = (ProductTypeProgramType)obj;
			return (castObj != null && this.productTypeId == castObj.ProductTypeId && this.programTypeId == castObj.ProgramTypeId);
		}

		/// <summary>
		/// Local implementation of GetHashCode based on unique value members
		/// </summary>
		public override int GetHashCode()
		{
			return 29 * (1 + this.productTypeId.GetHashCode() + this.programTypeId.GetHashCode());
		}
		#endregion
	}
}
