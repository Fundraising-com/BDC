////////////////////////////////////////////////////////////////////////////////////////////
// Class generated by SqlCodeGen 1.1.0.0.
// Do not edit this file directly. Changes will be lost when this file is regenerated.
// Extensions should be added in a separate file using partial classes.
////////////////////////////////////////////////////////////////////////////////////////////
using System;
using System.Collections.Generic;
using System.Data;
using System.Text;
using NHibernate;
using NHibernate.Criterion;
using NHibernate.Mapping.Attributes;

namespace QSP.Business.Fulfillment
{
	[Serializable]
	[Class(Schema="`dbo`", Table="`email_type`")]
	public partial class EmailType
	{
		#region Constants
        public const string EmailTypeEntity = "EmailType";
		public const string EmailTypeIdProperty = "EmailTypeId";
		public const string EmailTypeNameProperty = "EmailTypeName";
		#endregion

		#region Fields
		protected int emailTypeId = 0;
		protected string emailTypeName = "";
		#endregion

		#region Constructors
		public EmailType() 
		{
		}
		#endregion

		#region Properties
		[RawXml(Content=@"
		<id name=""EmailTypeId"" column=""`email_type_id`"">
			<generator class=""assigned"">
			</generator>
		</id>")]

		public virtual int EmailTypeId
		{
			get { return this.emailTypeId; }
			set { this.emailTypeId = value; }
		}

		[Property(Column="`email_type_name`")]
		public virtual string EmailTypeName
		{
			get { return this.emailTypeName; }
			set { this.emailTypeName = value; }
		}
		#endregion

		#region Methods

        public static ICriteria CreateCriteria()
        {
            using (ISession session = SqlSessionManager.OpenSession())
            {
                ICriteria c = session.CreateCriteria(typeof(EmailType));
                return c;
            }
        }

        public static List<EmailType> GetEmailTypeList(ICriteria criteria)
        {
            return (List<EmailType>)criteria.List<EmailType>();
        }

		public static EmailType GetEmailType(int emailTypeId)
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				return session.Get<EmailType>(emailTypeId);
			}
		}

		public static List<EmailType> GetEmailTypeList()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ICriteria c = session.CreateCriteria(typeof(EmailType));
				return (List<EmailType>)c.List<EmailType>();
			}
		}

		public static List<EmailType> GetEmailTypeList(string sortExpression, int maximumRows, int startRowIndex)
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ICriteria c = session.CreateCriteria(typeof(EmailType));
				if (sortExpression != null && sortExpression != "")
				{
					// Get ascending or descending order
					bool descending = sortExpression.Contains(" DESC");

					// Strip off ASC or DESC ordering
					sortExpression = sortExpression.Replace(" ASC", "");
					sortExpression = sortExpression.Replace(" DESC", "");
					sortExpression = sortExpression.Trim();

					// Get multi column sort from the comma delimited string
					List<String> expressions = new List<String>();
					if (sortExpression.Contains(","))
					{
						string[] tokens = sortExpression.Split(",".ToCharArray());
						for (int i = 0; i < tokens.Length; i++)
						{
							tokens[i] = tokens[i].Trim();
							if (tokens[i] != "")
								expressions.Add(tokens[i]);
						}
					}
					else if (sortExpression != "")
					{
						expressions.Add(sortExpression);
					}

					// Create the order
					for (int i = 0; i < expressions.Count; i++)
					{
						if (descending)
							c.AddOrder(NHibernate.Criterion.Order.Desc(expressions[i]));
						else
							c.AddOrder(NHibernate.Criterion.Order.Asc(expressions[i]));
					}
				}

				// Set offset and limit
				if (startRowIndex >= 0)
					c.SetFirstResult(startRowIndex);

				if (maximumRows >= 0)
					c.SetMaxResults(maximumRows);

				return (List<EmailType>)c.List<EmailType>();
			}
		}

		public static List<EmailType> GetEmailTypeList(string sortExpression)
		{
			return GetEmailTypeList(sortExpression, -1, -1);
		}

		public static void InsertEmailType(EmailType obj)
		{
			if (obj != null)
				obj.Insert();
		}

		public static void UpdateEmailType(EmailType obj)
		{
			if (obj != null)
				obj.Update();
		}

		public static void DeleteEmailType(EmailType obj)
		{
			if (obj != null)
				obj.Delete();
		}

        protected static EmailType PopulateEmailType(IDataReader r)
		{
			EmailType obj = new EmailType();
			obj.EmailTypeId = (int)r["email_type_id"];
			obj.EmailTypeName = (string)r["email_type_name"];

			return obj;
		}

		public static int GetCount()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ICriteria c = session.CreateCriteria(typeof(EmailType));
				c.SetProjection(Projections.RowCount());
				return (int) c.UniqueResult();
			}
		}

		/// <summary>
		/// Insert the entity to database.
		/// </summary>
		public virtual void Insert()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ITransaction trans = session.BeginTransaction();
				try
				{
					session.Save(this);
					trans.Commit();
				}
				catch
				{
					trans.Rollback();
					throw;
				}
			}
		}

		/// <summary>
		/// Update the entity to database.
		/// </summary>
		public virtual void Update()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ITransaction trans = session.BeginTransaction();
				try
				{
					session.Update(this);
					trans.Commit();
				}
				catch
				{
					trans.Rollback();
					throw;
				}
			}
		}

		/// <summary>
		/// Delete entity in database.
		/// </summary>
		public virtual void Delete()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ITransaction trans = session.BeginTransaction();
				try
				{
					session.Delete(this);
					trans.Commit();
				}
				catch
				{
					trans.Rollback();
					throw;
				}
			}
		}

		/// <summary>
		/// Local implementation of Equals based on unique value members
		/// </summary>
		public override bool Equals(object obj)
		{
			if (this == obj)
				return true;

			if ((obj == null) || (obj.GetType() != this.GetType())) 
				return false;

			EmailType castObj = (EmailType)obj;
			return (castObj != null && this.emailTypeId == castObj.EmailTypeId);
		}

		/// <summary>
		/// Local implementation of GetHashCode based on unique value members
		/// </summary>
		public override int GetHashCode()
		{
			return 29 * (1 + this.emailTypeId.GetHashCode());
		}
		#endregion
	}
}
