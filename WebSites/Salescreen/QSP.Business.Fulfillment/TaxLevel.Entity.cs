////////////////////////////////////////////////////////////////////////////////////////////
// Class generated by SqlCodeGen 1.1.0.0.
// Do not edit this file directly. Changes will be lost when this file is regenerated.
// Extensions should be added in a separate file using partial classes.
////////////////////////////////////////////////////////////////////////////////////////////
using System;
using System.Collections.Generic;
using System.Data;
using System.Text;
using NHibernate;
using NHibernate.Criterion;
using NHibernate.Mapping.Attributes;

namespace QSP.Business.Fulfillment
{
	[Serializable]
	[Class(Schema="`dbo`", Table="`tax_level`")]
	public partial class TaxLevel
	{
		#region Constants
        public const string TaxLevelEntity = "TaxLevel";
		public const string TaxLevelIdProperty = "TaxLevelId";
		public const string TaxLevelNameProperty = "TaxLevelName";
		#endregion

		#region Fields
		protected int taxLevelId = 0;
		protected string taxLevelName = "";
		#endregion

		#region Constructors
		public TaxLevel() 
		{
		}
		#endregion

		#region Properties
		[RawXml(Content=@"
		<id name=""TaxLevelId"" column=""`tax_level_id`"">
			<generator class=""assigned"">
			</generator>
		</id>")]

		public virtual int TaxLevelId
		{
			get { return this.taxLevelId; }
			set { this.taxLevelId = value; }
		}

		[Property(Column="`tax_level_name`")]
		public virtual string TaxLevelName
		{
			get { return this.taxLevelName; }
			set { this.taxLevelName = value; }
		}
		#endregion

		#region Methods

        public static ICriteria CreateCriteria()
        {
            using (ISession session = SqlSessionManager.OpenSession())
            {
                ICriteria c = session.CreateCriteria(typeof(TaxLevel));
                return c;
            }
        }

        public static List<TaxLevel> GetTaxLevelList(ICriteria criteria)
        {
            return (List<TaxLevel>)criteria.List<TaxLevel>();
        }

		public static TaxLevel GetTaxLevel(int taxLevelId)
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				return session.Get<TaxLevel>(taxLevelId);
			}
		}

		public static List<TaxLevel> GetTaxLevelList()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ICriteria c = session.CreateCriteria(typeof(TaxLevel));
				return (List<TaxLevel>)c.List<TaxLevel>();
			}
		}

		public static List<TaxLevel> GetTaxLevelList(string sortExpression, int maximumRows, int startRowIndex)
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ICriteria c = session.CreateCriteria(typeof(TaxLevel));
				if (sortExpression != null && sortExpression != "")
				{
					// Get ascending or descending order
					bool descending = sortExpression.Contains(" DESC");

					// Strip off ASC or DESC ordering
					sortExpression = sortExpression.Replace(" ASC", "");
					sortExpression = sortExpression.Replace(" DESC", "");
					sortExpression = sortExpression.Trim();

					// Get multi column sort from the comma delimited string
					List<String> expressions = new List<String>();
					if (sortExpression.Contains(","))
					{
						string[] tokens = sortExpression.Split(",".ToCharArray());
						for (int i = 0; i < tokens.Length; i++)
						{
							tokens[i] = tokens[i].Trim();
							if (tokens[i] != "")
								expressions.Add(tokens[i]);
						}
					}
					else if (sortExpression != "")
					{
						expressions.Add(sortExpression);
					}

					// Create the order
					for (int i = 0; i < expressions.Count; i++)
					{
						if (descending)
							c.AddOrder(NHibernate.Criterion.Order.Desc(expressions[i]));
						else
							c.AddOrder(NHibernate.Criterion.Order.Asc(expressions[i]));
					}
				}

				// Set offset and limit
				if (startRowIndex >= 0)
					c.SetFirstResult(startRowIndex);

				if (maximumRows >= 0)
					c.SetMaxResults(maximumRows);

				return (List<TaxLevel>)c.List<TaxLevel>();
			}
		}

		public static List<TaxLevel> GetTaxLevelList(string sortExpression)
		{
			return GetTaxLevelList(sortExpression, -1, -1);
		}

		public static void InsertTaxLevel(TaxLevel obj)
		{
			if (obj != null)
				obj.Insert();
		}

		public static void UpdateTaxLevel(TaxLevel obj)
		{
			if (obj != null)
				obj.Update();
		}

		public static void DeleteTaxLevel(TaxLevel obj)
		{
			if (obj != null)
				obj.Delete();
		}

        protected static TaxLevel PopulateTaxLevel(IDataReader r)
		{
			TaxLevel obj = new TaxLevel();
			obj.TaxLevelId = (int)r["tax_level_id"];
			obj.TaxLevelName = (string)r["tax_level_name"];

			return obj;
		}

		public static int GetCount()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ICriteria c = session.CreateCriteria(typeof(TaxLevel));
				c.SetProjection(Projections.RowCount());
				return (int) c.UniqueResult();
			}
		}

		/// <summary>
		/// Insert the entity to database.
		/// </summary>
		public virtual void Insert()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ITransaction trans = session.BeginTransaction();
				try
				{
					session.Save(this);
					trans.Commit();
				}
				catch
				{
					trans.Rollback();
					throw;
				}
			}
		}

		/// <summary>
		/// Update the entity to database.
		/// </summary>
		public virtual void Update()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ITransaction trans = session.BeginTransaction();
				try
				{
					session.Update(this);
					trans.Commit();
				}
				catch
				{
					trans.Rollback();
					throw;
				}
			}
		}

		/// <summary>
		/// Delete entity in database.
		/// </summary>
		public virtual void Delete()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ITransaction trans = session.BeginTransaction();
				try
				{
					session.Delete(this);
					trans.Commit();
				}
				catch
				{
					trans.Rollback();
					throw;
				}
			}
		}

		/// <summary>
		/// Local implementation of Equals based on unique value members
		/// </summary>
		public override bool Equals(object obj)
		{
			if (this == obj)
				return true;

			if ((obj == null) || (obj.GetType() != this.GetType())) 
				return false;

			TaxLevel castObj = (TaxLevel)obj;
			return (castObj != null && this.taxLevelId == castObj.TaxLevelId);
		}

		/// <summary>
		/// Local implementation of GetHashCode based on unique value members
		/// </summary>
		public override int GetHashCode()
		{
			return 29 * (1 + this.taxLevelId.GetHashCode());
		}
		#endregion
	}
}
