////////////////////////////////////////////////////////////////////////////////////////////
// Class generated by SqlCodeGen 1.1.0.0.
// Do not edit this file directly. Changes will be lost when this file is regenerated.
// Extensions should be added in a separate file using partial classes.
////////////////////////////////////////////////////////////////////////////////////////////
using System;
using System.Collections.Generic;
using System.Data;
using System.Text;
using NHibernate;
using NHibernate.Criterion;
using NHibernate.Mapping.Attributes;

namespace QSP.Business.Fulfillment
{
	[Serializable]
	[Class(Schema="`dbo`", Table="`favorite_logo`")]
	public partial class FavoriteLogo
	{
		#region Constants
        public const string FavoriteLogoEntity = "FavoriteLogo";
		public const string FavoriteLogoIdProperty = "FavoriteLogoId";
		public const string FieldSalesManagerIdProperty = "FieldSalesManagerId";
		public const string LogoIdProperty = "LogoId";
		#endregion

		#region Fields
		protected int favoriteLogoId = 0;
		protected int? fieldSalesManagerId = null;
		protected int logoId = 0;
		#endregion

		#region Constructors
		public FavoriteLogo() 
		{
		}
		#endregion

		#region Properties
		[RawXml(Content=@"
		<id name=""FavoriteLogoId"" column=""`favorite_logo_id`"">
			<generator class=""native"">
			</generator>
		</id>")]

		public virtual int FavoriteLogoId
		{
			get { return this.favoriteLogoId; }
			set { this.favoriteLogoId = value; }
		}

		[Property(Column="`field_sales_manager_id`")]
		public virtual int? FieldSalesManagerId
		{
			get { return this.fieldSalesManagerId; }
			set { this.fieldSalesManagerId = value; }
		}

		[Property(Column="`logo_id`")]
		public virtual int LogoId
		{
			get { return this.logoId; }
			set { this.logoId = value; }
		}
		#endregion

		#region Methods

        public static ICriteria CreateCriteria()
        {
            using (ISession session = SqlSessionManager.OpenSession())
            {
                ICriteria c = session.CreateCriteria(typeof(FavoriteLogo));
                return c;
            }
        }

        public static List<FavoriteLogo> GetFavoriteLogoList(ICriteria criteria)
        {
            return (List<FavoriteLogo>)criteria.List<FavoriteLogo>();
        }

		public static FavoriteLogo GetFavoriteLogo(int favoriteLogoId)
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				return session.Get<FavoriteLogo>(favoriteLogoId);
			}
		}

		public static List<FavoriteLogo> GetFavoriteLogoList()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ICriteria c = session.CreateCriteria(typeof(FavoriteLogo));
				return (List<FavoriteLogo>)c.List<FavoriteLogo>();
			}
		}

		public static List<FavoriteLogo> GetFavoriteLogoList(string sortExpression, int maximumRows, int startRowIndex)
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ICriteria c = session.CreateCriteria(typeof(FavoriteLogo));
				if (sortExpression != null && sortExpression != "")
				{
					// Get ascending or descending order
					bool descending = sortExpression.Contains(" DESC");

					// Strip off ASC or DESC ordering
					sortExpression = sortExpression.Replace(" ASC", "");
					sortExpression = sortExpression.Replace(" DESC", "");
					sortExpression = sortExpression.Trim();

					// Get multi column sort from the comma delimited string
					List<String> expressions = new List<String>();
					if (sortExpression.Contains(","))
					{
						string[] tokens = sortExpression.Split(",".ToCharArray());
						for (int i = 0; i < tokens.Length; i++)
						{
							tokens[i] = tokens[i].Trim();
							if (tokens[i] != "")
								expressions.Add(tokens[i]);
						}
					}
					else if (sortExpression != "")
					{
						expressions.Add(sortExpression);
					}

					// Create the order
					for (int i = 0; i < expressions.Count; i++)
					{
						if (descending)
							c.AddOrder(NHibernate.Criterion.Order.Desc(expressions[i]));
						else
							c.AddOrder(NHibernate.Criterion.Order.Asc(expressions[i]));
					}
				}

				// Set offset and limit
				if (startRowIndex >= 0)
					c.SetFirstResult(startRowIndex);

				if (maximumRows >= 0)
					c.SetMaxResults(maximumRows);

				return (List<FavoriteLogo>)c.List<FavoriteLogo>();
			}
		}

		public static List<FavoriteLogo> GetFavoriteLogoList(string sortExpression)
		{
			return GetFavoriteLogoList(sortExpression, -1, -1);
		}

		public static void InsertFavoriteLogo(FavoriteLogo obj)
		{
			if (obj != null)
				obj.Insert();
		}

		public static void UpdateFavoriteLogo(FavoriteLogo obj)
		{
			if (obj != null)
				obj.Update();
		}

		public static void DeleteFavoriteLogo(FavoriteLogo obj)
		{
			if (obj != null)
				obj.Delete();
		}

        protected static FavoriteLogo PopulateFavoriteLogo(IDataReader r)
		{
			FavoriteLogo obj = new FavoriteLogo();
			obj.FavoriteLogoId = (int)r["favorite_logo_id"];
			obj.FieldSalesManagerId = (r["field_sales_manager_id"] == DBNull.Value) ? null : (int?)r["field_sales_manager_id"];
			obj.LogoId = (int)r["logo_id"];

			return obj;
		}

		public static int GetCount()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ICriteria c = session.CreateCriteria(typeof(FavoriteLogo));
				c.SetProjection(Projections.RowCount());
				return (int) c.UniqueResult();
			}
		}

		/// <summary>
		/// Insert the entity to database.
		/// </summary>
		public virtual void Insert()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ITransaction trans = session.BeginTransaction();
				try
				{
					session.Save(this);
					trans.Commit();
				}
				catch
				{
					trans.Rollback();
					throw;
				}
			}
		}

		/// <summary>
		/// Update the entity to database.
		/// </summary>
		public virtual void Update()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ITransaction trans = session.BeginTransaction();
				try
				{
					session.Update(this);
					trans.Commit();
				}
				catch
				{
					trans.Rollback();
					throw;
				}
			}
		}

		/// <summary>
		/// Persist the entity back to database.
		/// </summary>
		public virtual void Save()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ITransaction trans = session.BeginTransaction();
				try
				{
					session.SaveOrUpdate(this);
					trans.Commit();
				}
				catch
				{
					trans.Rollback();
					throw;
				}
			}
		}

		/// <summary>
		/// Delete entity in database.
		/// </summary>
		public virtual void Delete()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ITransaction trans = session.BeginTransaction();
				try
				{
					session.Delete(this);
					trans.Commit();
				}
				catch
				{
					trans.Rollback();
					throw;
				}
			}
		}

		/// <summary>
		/// Local implementation of Equals based on unique value members
		/// </summary>
		public override bool Equals(object obj)
		{
			if (this == obj)
				return true;

			if ((obj == null) || (obj.GetType() != this.GetType())) 
				return false;

			FavoriteLogo castObj = (FavoriteLogo)obj;
			return (castObj != null && this.favoriteLogoId == castObj.FavoriteLogoId);
		}

		/// <summary>
		/// Local implementation of GetHashCode based on unique value members
		/// </summary>
		public override int GetHashCode()
		{
			return 29 * (1 + this.favoriteLogoId.GetHashCode());
		}
		#endregion
	}
}
