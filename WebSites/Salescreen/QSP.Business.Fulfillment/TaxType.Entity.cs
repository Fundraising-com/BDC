////////////////////////////////////////////////////////////////////////////////////////////
// Class generated by SqlCodeGen 1.1.0.0.
// Do not edit this file directly. Changes will be lost when this file is regenerated.
// Extensions should be added in a separate file using partial classes.
////////////////////////////////////////////////////////////////////////////////////////////
using System;
using System.Collections.Generic;
using System.Data;
using System.Text;
using NHibernate;
using NHibernate.Criterion;
using NHibernate.Mapping.Attributes;

namespace QSP.Business.Fulfillment
{
	[Serializable]
	[Class(Schema="`dbo`", Table="`tax_type`")]
	public partial class TaxType
	{
		#region Constants
        public const string TaxTypeEntity = "TaxType";
		public const string TaxTypeIdProperty = "TaxTypeId";
		public const string TaxTypeNameProperty = "TaxTypeName";
		#endregion

		#region Fields
		protected int taxTypeId = 0;
		protected string taxTypeName = "";
		#endregion

		#region Constructors
		public TaxType() 
		{
		}
		#endregion

		#region Properties
		[RawXml(Content=@"
		<id name=""TaxTypeId"" column=""`tax_type_id`"">
			<generator class=""assigned"">
			</generator>
		</id>")]

		public virtual int TaxTypeId
		{
			get { return this.taxTypeId; }
			set { this.taxTypeId = value; }
		}

		[Property(Column="`tax_type_name`")]
		public virtual string TaxTypeName
		{
			get { return this.taxTypeName; }
			set { this.taxTypeName = value; }
		}
		#endregion

		#region Methods

        public static ICriteria CreateCriteria()
        {
            using (ISession session = SqlSessionManager.OpenSession())
            {
                ICriteria c = session.CreateCriteria(typeof(TaxType));
                return c;
            }
        }

        public static List<TaxType> GetTaxTypeList(ICriteria criteria)
        {
            return (List<TaxType>)criteria.List<TaxType>();
        }

		public static TaxType GetTaxType(int taxTypeId)
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				return session.Get<TaxType>(taxTypeId);
			}
		}

		public static List<TaxType> GetTaxTypeList()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ICriteria c = session.CreateCriteria(typeof(TaxType));
				return (List<TaxType>)c.List<TaxType>();
			}
		}

		public static List<TaxType> GetTaxTypeList(string sortExpression, int maximumRows, int startRowIndex)
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ICriteria c = session.CreateCriteria(typeof(TaxType));
				if (sortExpression != null && sortExpression != "")
				{
					// Get ascending or descending order
					bool descending = sortExpression.Contains(" DESC");

					// Strip off ASC or DESC ordering
					sortExpression = sortExpression.Replace(" ASC", "");
					sortExpression = sortExpression.Replace(" DESC", "");
					sortExpression = sortExpression.Trim();

					// Get multi column sort from the comma delimited string
					List<String> expressions = new List<String>();
					if (sortExpression.Contains(","))
					{
						string[] tokens = sortExpression.Split(",".ToCharArray());
						for (int i = 0; i < tokens.Length; i++)
						{
							tokens[i] = tokens[i].Trim();
							if (tokens[i] != "")
								expressions.Add(tokens[i]);
						}
					}
					else if (sortExpression != "")
					{
						expressions.Add(sortExpression);
					}

					// Create the order
					for (int i = 0; i < expressions.Count; i++)
					{
						if (descending)
							c.AddOrder(NHibernate.Criterion.Order.Desc(expressions[i]));
						else
							c.AddOrder(NHibernate.Criterion.Order.Asc(expressions[i]));
					}
				}

				// Set offset and limit
				if (startRowIndex >= 0)
					c.SetFirstResult(startRowIndex);

				if (maximumRows >= 0)
					c.SetMaxResults(maximumRows);

				return (List<TaxType>)c.List<TaxType>();
			}
		}

		public static List<TaxType> GetTaxTypeList(string sortExpression)
		{
			return GetTaxTypeList(sortExpression, -1, -1);
		}

		public static void InsertTaxType(TaxType obj)
		{
			if (obj != null)
				obj.Insert();
		}

		public static void UpdateTaxType(TaxType obj)
		{
			if (obj != null)
				obj.Update();
		}

		public static void DeleteTaxType(TaxType obj)
		{
			if (obj != null)
				obj.Delete();
		}

        protected static TaxType PopulateTaxType(IDataReader r)
		{
			TaxType obj = new TaxType();
			obj.TaxTypeId = (int)r["tax_type_id"];
			obj.TaxTypeName = (string)r["tax_type_name"];

			return obj;
		}

		public static int GetCount()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ICriteria c = session.CreateCriteria(typeof(TaxType));
				c.SetProjection(Projections.RowCount());
				return (int) c.UniqueResult();
			}
		}

		/// <summary>
		/// Insert the entity to database.
		/// </summary>
		public virtual void Insert()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ITransaction trans = session.BeginTransaction();
				try
				{
					session.Save(this);
					trans.Commit();
				}
				catch
				{
					trans.Rollback();
					throw;
				}
			}
		}

		/// <summary>
		/// Update the entity to database.
		/// </summary>
		public virtual void Update()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ITransaction trans = session.BeginTransaction();
				try
				{
					session.Update(this);
					trans.Commit();
				}
				catch
				{
					trans.Rollback();
					throw;
				}
			}
		}

		/// <summary>
		/// Delete entity in database.
		/// </summary>
		public virtual void Delete()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ITransaction trans = session.BeginTransaction();
				try
				{
					session.Delete(this);
					trans.Commit();
				}
				catch
				{
					trans.Rollback();
					throw;
				}
			}
		}

		/// <summary>
		/// Local implementation of Equals based on unique value members
		/// </summary>
		public override bool Equals(object obj)
		{
			if (this == obj)
				return true;

			if ((obj == null) || (obj.GetType() != this.GetType())) 
				return false;

			TaxType castObj = (TaxType)obj;
			return (castObj != null && this.taxTypeId == castObj.TaxTypeId);
		}

		/// <summary>
		/// Local implementation of GetHashCode based on unique value members
		/// </summary>
		public override int GetHashCode()
		{
			return 29 * (1 + this.taxTypeId.GetHashCode());
		}
		#endregion
	}
}
