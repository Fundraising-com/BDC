////////////////////////////////////////////////////////////////////////////////////////////
// Class generated by SqlCodeGen 1.1.0.0.
// Do not edit this file directly. Changes will be lost when this file is regenerated.
// Extensions should be added in a separate file using partial classes.
////////////////////////////////////////////////////////////////////////////////////////////
using System;
using System.Collections.Generic;
using System.Data;
using System.Text;
using NHibernate;
using NHibernate.Criterion;
using NHibernate.Mapping.Attributes;

namespace QSP.Business.Fulfillment
{
	[Serializable]
	[Class(Schema="`dbo`", Table="`promotion`")]
	public partial class Promotion
	{
		#region Constants
        public const string PromotionEntity = "Promotion";
		public const string PromotionIdProperty = "PromotionId";
		public const string PromotionNameProperty = "PromotionName";
		#endregion

		#region Fields
		protected int promotionId = 0;
		protected string promotionName = "";
		#endregion

		#region Constructors
		public Promotion() 
		{
		}
		#endregion

		#region Properties
		[RawXml(Content=@"
		<id name=""PromotionId"" column=""`promotion_id`"">
			<generator class=""assigned"">
			</generator>
		</id>")]

		public virtual int PromotionId
		{
			get { return this.promotionId; }
			set { this.promotionId = value; }
		}

		[Property(Column="`promotion_name`")]
		public virtual string PromotionName
		{
			get { return this.promotionName; }
			set { this.promotionName = value; }
		}
		#endregion

		#region Methods

        public static ICriteria CreateCriteria()
        {
            using (ISession session = SqlSessionManager.OpenSession())
            {
                ICriteria c = session.CreateCriteria(typeof(Promotion));
                return c;
            }
        }

        public static List<Promotion> GetPromotionList(ICriteria criteria)
        {
            return (List<Promotion>)criteria.List<Promotion>();
        }

		public static Promotion GetPromotion(int promotionId)
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				return session.Get<Promotion>(promotionId);
			}
		}

		public static List<Promotion> GetPromotionList()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ICriteria c = session.CreateCriteria(typeof(Promotion));
				return (List<Promotion>)c.List<Promotion>();
			}
		}

		public static List<Promotion> GetPromotionList(string sortExpression, int maximumRows, int startRowIndex)
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ICriteria c = session.CreateCriteria(typeof(Promotion));
				if (sortExpression != null && sortExpression != "")
				{
					// Get ascending or descending order
					bool descending = sortExpression.Contains(" DESC");

					// Strip off ASC or DESC ordering
					sortExpression = sortExpression.Replace(" ASC", "");
					sortExpression = sortExpression.Replace(" DESC", "");
					sortExpression = sortExpression.Trim();

					// Get multi column sort from the comma delimited string
					List<String> expressions = new List<String>();
					if (sortExpression.Contains(","))
					{
						string[] tokens = sortExpression.Split(",".ToCharArray());
						for (int i = 0; i < tokens.Length; i++)
						{
							tokens[i] = tokens[i].Trim();
							if (tokens[i] != "")
								expressions.Add(tokens[i]);
						}
					}
					else if (sortExpression != "")
					{
						expressions.Add(sortExpression);
					}

					// Create the order
					for (int i = 0; i < expressions.Count; i++)
					{
						if (descending)
							c.AddOrder(NHibernate.Criterion.Order.Desc(expressions[i]));
						else
							c.AddOrder(NHibernate.Criterion.Order.Asc(expressions[i]));
					}
				}

				// Set offset and limit
				if (startRowIndex >= 0)
					c.SetFirstResult(startRowIndex);

				if (maximumRows >= 0)
					c.SetMaxResults(maximumRows);

				return (List<Promotion>)c.List<Promotion>();
			}
		}

		public static List<Promotion> GetPromotionList(string sortExpression)
		{
			return GetPromotionList(sortExpression, -1, -1);
		}

		public static void InsertPromotion(Promotion obj)
		{
			if (obj != null)
				obj.Insert();
		}

		public static void UpdatePromotion(Promotion obj)
		{
			if (obj != null)
				obj.Update();
		}

		public static void DeletePromotion(Promotion obj)
		{
			if (obj != null)
				obj.Delete();
		}

        protected static Promotion PopulatePromotion(IDataReader r)
		{
			Promotion obj = new Promotion();
			obj.PromotionId = (int)r["promotion_id"];
			obj.PromotionName = (string)r["promotion_name"];

			return obj;
		}

		public static int GetCount()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ICriteria c = session.CreateCriteria(typeof(Promotion));
				c.SetProjection(Projections.RowCount());
				return (int) c.UniqueResult();
			}
		}

		/// <summary>
		/// Insert the entity to database.
		/// </summary>
		public virtual void Insert()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ITransaction trans = session.BeginTransaction();
				try
				{
					session.Save(this);
					trans.Commit();
				}
				catch
				{
					trans.Rollback();
					throw;
				}
			}
		}

		/// <summary>
		/// Update the entity to database.
		/// </summary>
		public virtual void Update()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ITransaction trans = session.BeginTransaction();
				try
				{
					session.Update(this);
					trans.Commit();
				}
				catch
				{
					trans.Rollback();
					throw;
				}
			}
		}

		/// <summary>
		/// Delete entity in database.
		/// </summary>
		public virtual void Delete()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ITransaction trans = session.BeginTransaction();
				try
				{
					session.Delete(this);
					trans.Commit();
				}
				catch
				{
					trans.Rollback();
					throw;
				}
			}
		}

		/// <summary>
		/// Local implementation of Equals based on unique value members
		/// </summary>
		public override bool Equals(object obj)
		{
			if (this == obj)
				return true;

			if ((obj == null) || (obj.GetType() != this.GetType())) 
				return false;

			Promotion castObj = (Promotion)obj;
			return (castObj != null && this.promotionId == castObj.PromotionId);
		}

		/// <summary>
		/// Local implementation of GetHashCode based on unique value members
		/// </summary>
		public override int GetHashCode()
		{
			return 29 * (1 + this.promotionId.GetHashCode());
		}
		#endregion
	}
}
