////////////////////////////////////////////////////////////////////////////////////////////
// Class generated by SqlCodeGen 1.1.0.0.
// Do not edit this file directly. Changes will be lost when this file is regenerated.
// Extensions should be added in a separate file using partial classes.
////////////////////////////////////////////////////////////////////////////////////////////
using System;
using System.Collections.Generic;
using System.Data;
using System.Text;
using NHibernate;
using NHibernate.Criterion;
using NHibernate.Mapping.Attributes;

namespace QSP.Business.Fulfillment
{
	[Serializable]
	[Class(Schema="`dbo`", Table="`payment_code`")]
	public partial class PaymentCode
	{
		#region Constants
        public const string PaymentCodeEntity = "PaymentCode";
		public const string PaymentCodeIdProperty = "PaymentCodeId";
		public const string PaymentCodeNameProperty = "PaymentCodeName";
		#endregion

		#region Fields
		protected int paymentCodeId = 0;
		protected string paymentCodeName = "";
		#endregion

		#region Constructors
		public PaymentCode() 
		{
		}
		#endregion

		#region Properties
		[RawXml(Content=@"
		<id name=""PaymentCodeId"" column=""`payment_code_id`"">
			<generator class=""native"">
			</generator>
		</id>")]

		public virtual int PaymentCodeId
		{
			get { return this.paymentCodeId; }
			set { this.paymentCodeId = value; }
		}

		[Property(Column="`payment_code_name`")]
		public virtual string PaymentCodeName
		{
			get { return this.paymentCodeName; }
			set { this.paymentCodeName = value; }
		}
		#endregion

		#region Methods
        public static ICriteria CreateCriteria()
        {
            using (ISession session = SqlSessionManager.OpenSession())
            {
                ICriteria c = session.CreateCriteria(typeof(PaymentType));
                return c;
            }
        }

        public static List<PaymentType> GetPaymentCodeList(ICriteria criteria)
        {
            return (List<PaymentType>)criteria.List<PaymentType>();
        }

		public static PaymentCode GetPaymentCode(int paymentCodeId)
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				return session.Get<PaymentCode>(paymentCodeId);
			}
		}

		public static List<PaymentCode> GetPaymentCodeList()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ICriteria c = session.CreateCriteria(typeof(PaymentCode));
				return (List<PaymentCode>)c.List<PaymentCode>();
			}
		}

		public static List<PaymentCode> GetPaymentCodeList(string sortExpression, int maximumRows, int startRowIndex)
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ICriteria c = session.CreateCriteria(typeof(PaymentCode));
				if (sortExpression != null && sortExpression != "")
				{
					// Get ascending or descending order
					bool descending = sortExpression.Contains(" DESC");

					// Strip off ASC or DESC ordering
					sortExpression = sortExpression.Replace(" ASC", "");
					sortExpression = sortExpression.Replace(" DESC", "");
					sortExpression = sortExpression.Trim();

					// Get multi column sort from the comma delimited string
					List<String> expressions = new List<String>();
					if (sortExpression.Contains(","))
					{
						string[] tokens = sortExpression.Split(",".ToCharArray());
						for (int i = 0; i < tokens.Length; i++)
						{
							tokens[i] = tokens[i].Trim();
							if (tokens[i] != "")
								expressions.Add(tokens[i]);
						}
					}
					else if (sortExpression != "")
					{
						expressions.Add(sortExpression);
					}

					// Create the order
					for (int i = 0; i < expressions.Count; i++)
					{
						if (descending)
							c.AddOrder(NHibernate.Criterion.Order.Desc(expressions[i]));
						else
							c.AddOrder(NHibernate.Criterion.Order.Asc(expressions[i]));
					}
				}

				// Set offset and limit
				if (startRowIndex >= 0)
					c.SetFirstResult(startRowIndex);

				if (maximumRows >= 0)
					c.SetMaxResults(maximumRows);

				return (List<PaymentCode>)c.List<PaymentCode>();
			}
		}

		public static List<PaymentCode> GetPaymentCodeList(string sortExpression)
		{
			return GetPaymentCodeList(sortExpression, -1, -1);
		}

		public static void InsertPaymentCode(PaymentCode obj)
		{
			if (obj != null)
				obj.Insert();
		}

		public static void UpdatePaymentCode(PaymentCode obj)
		{
			if (obj != null)
				obj.Update();
		}

		public static void DeletePaymentCode(PaymentCode obj)
		{
			if (obj != null)
				obj.Delete();
		}

        protected static PaymentCode PopulatePaymentCode(IDataReader r)
		{
			PaymentCode obj = new PaymentCode();
			obj.PaymentCodeId = (int)r["payment_code_id"];
			obj.PaymentCodeName = (string)r["payment_code_name"];

			return obj;
		}

		public static int GetCount()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ICriteria c = session.CreateCriteria(typeof(PaymentCode));
				c.SetProjection(Projections.RowCount());
				return (int) c.UniqueResult();
			}
		}

		/// <summary>
		/// Insert the entity to database.
		/// </summary>
		public virtual void Insert()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ITransaction trans = session.BeginTransaction();
				try
				{
					session.Save(this);
					trans.Commit();
				}
				catch
				{
					trans.Rollback();
					throw;
				}
			}
		}

		/// <summary>
		/// Update the entity to database.
		/// </summary>
		public virtual void Update()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ITransaction trans = session.BeginTransaction();
				try
				{
					session.Update(this);
					trans.Commit();
				}
				catch
				{
					trans.Rollback();
					throw;
				}
			}
		}

		/// <summary>
		/// Persist the entity back to database.
		/// </summary>
		public virtual void Save()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ITransaction trans = session.BeginTransaction();
				try
				{
					session.SaveOrUpdate(this);
					trans.Commit();
				}
				catch
				{
					trans.Rollback();
					throw;
				}
			}
		}

		/// <summary>
		/// Delete entity in database.
		/// </summary>
		public virtual void Delete()
		{
			using (ISession session = SqlSessionManager.OpenSession())
			{
				ITransaction trans = session.BeginTransaction();
				try
				{
					session.Delete(this);
					trans.Commit();
				}
				catch
				{
					trans.Rollback();
					throw;
				}
			}
		}

		/// <summary>
		/// Local implementation of Equals based on unique value members
		/// </summary>
		public override bool Equals(object obj)
		{
			if (this == obj)
				return true;

			if ((obj == null) || (obj.GetType() != this.GetType())) 
				return false;

			PaymentCode castObj = (PaymentCode)obj;
			return (castObj != null && this.paymentCodeId == castObj.PaymentCodeId);
		}

		/// <summary>
		/// Local implementation of GetHashCode based on unique value members
		/// </summary>
		public override int GetHashCode()
		{
			return 29 * (1 + this.paymentCodeId.GetHashCode());
		}
		#endregion
	}
}
